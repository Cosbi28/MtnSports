@using Microsoft.AspNetCore.Identity
@using System.Security.Claims
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@model DataModels.Order

@{
    ViewData["Title"] = "Return";
}
<div class="container">
    <main role="main" class="pb-3">

        <h1>Cancel order</h1>

        <h3>Do you confirm returning the order?</h3>
        <div>
            <hr />
            <dl class="row">
                @*<dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.IdUser)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.IdUser)
                </dd>*@
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.PickupDate)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.PickupDate)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.ReturnDate)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.ReturnDate)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.TotalPrice)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.TotalPrice)
                </dd>

            </dl>

            <form asp-action="Return">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="Return" class="btn btn-danger" asp-route-userId=@User.FindFirst(ClaimTypes.NameIdentifier).Value/> |
                <a asp-action="Index" asp-route-userId=@User.FindFirst(ClaimTypes.NameIdentifier).Value class="btn btn-info">Back to List</a>
            </form>
        </div>

    </main>
</div>